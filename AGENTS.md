# ds-asv — Agent Instructions

## Project state (verified 2026-08-30)

- **What:** Commercial PCI DSS compliance portal. SaaS, multi-tenant (nested orgs — QSA resellers + merchants).
- **Stack:** Next.js/TS portal (`portal/`) + Python/FastAPI scanner (`scanner/`) + PostgreSQL (RLS). Self-hosted Keycloak (OIDC), Vault, MinIO/S3.
- **Repo:** https://github.com/fibz/ds-asv — public, branch `main`. Pushed to `origin/main` at `63f2daf` (2026-09-06, follow-ups: tsc clean, scanner banner alias + refresh, cookie-session registry, secret bootstrap); local `main` in sync with `origin/main`. Phase 5b NVD-mirror design spec (`98b2883`) is part of history.
- **Phase 1 DONE** (tenant & identity foundation): RLS multi-tenancy (non-superuser `asv_app` role), Keycloak OIDC header auth + salted API keys, RBAC (`hasRole`/`can`/`requireRole`), single-use expiring invitations, append-only audit (DB-enforced), cross-tenant isolation tests.
- **Phase 2 DONE** (asset inventory): Asset/AssetVerification/AssetImport models + RLS, identifier normalization (ipv4/ipv6/cidr/fqdn), CSV import w/ preview + downloadable invalid rows + idempotency, dedupe (duplicates never create extra assets), lifecycle/retire (referenced-asset history preserved — row + audit + verification, never hard-deleted), DNS TXT/manual verification, API routes (CRUD/import/retire/verification), assets UI (list/filter/detail + import), ApiKey RLS revival. **273/273 tests green** (`npx vitest run` in `portal/`).
- **Design docs:** `docs/superpowers/specs/2026-08-29-ds-asv-pci-portal-design.md` (approved). Phase 1 plan: `docs/superpowers/plans/2026-08-29-phase1-tenant-identity.md`. Phase 2 plan: `docs/superpowers/plans/2026-08-30-phase2-asset-inventory.md`.
- **User Center DONE** (hub): org profile + contacts, team management (members/roles/removal w/ last-owner guard), session registry (hash-only tokens, revoke = blocked at auth), audit trail API + UI baseline (`/settings`, `/team`, `/access`, `/audit`). API contract: `portal/spec/openapi.yaml` (org/team/sessions/audit/invitations) — handoff artifact for the UI stream (Codex / UI dev).
- **Phase 3 DONE** (scans + scan reports): Scan/ScanTarget/Finding/Report(+Attestation) models + RLS, scan creation from selected assets (lightweight scope snapshot), HMAC-signed expiring scan manifest, findings ingestion (fingerprint-deduped), Qualys-style report generation, QA attestation gate (report final only when attested in prod), scan/report API + contract. Simulated scanner (test double) drives the dev/test loop.
- **Phase 3b DONE** (scanner service integration): real Python scanner (`scanner/`) wired to the Phase 3 manifest + ingestion contract. It consumes `POST /v1/manifests`, verifies the signed manifest, runs a black-box scan per target, maps findings, and writes findings + lifecycle back to the portal (post + patch). Portal full suite: 278 tests (`npx vitest run` in `portal/`). Scanner full suite green (`pytest tests/` in `scanner/`).
- **Phase 4 DONE** (versioned scope & authorization + dispute flow): ScopeSet/ScopeVersion/ScopeItem (immutable snapshots, draft→submitted→approved, deterministic content hash) + Authorization (HMAC-signed statement + scope-version hash, verifiable) + Dispute models, all with RLS; scope service + prod scope-approval gate on scan creation (no scan without an approved scope version — assetInApprovedScope); signed authorization issuance/verification; dispute flow (raise w/ justification, QA staff moderation with a prod staff gate); scope/auth/dispute API routes + contract; exit criteria (gate → approved scan → signed authorization → staff moderation end-to-end + contract conformance). Portal full suite: 322 tests (`npx vitest run` in `portal/`).
- **Phase 5 DONE** (scanning UI wiring + report finalization gate): assets → scope → scan flow (`/scanners`), scope management UI (`/scope` — sets, immutable snapshots, submit/approve), reports UI with the finalization gate badge (`/reports`); service-layer report finalization gate = report final only when QA-attested AND its scope version is approved (`isReportFinal` strict semantics + prod attest scope guard in `report.ts`); `buildReport` records the approved scope version behind each report. Exit criteria: approved scope → gated scan → finding → report final only when attested AND scope approved (end-to-end in `scope/exit.test.ts`). Portal full suite: 335 tests (`npx vitest run` in `portal/`).
- **Phase 6 DONE** (staff identity → prod attestation/dispute moderation reachable): `KeycloakUser` carries `roles` (parsed from the verified `realm_access.roles` claim, `realmRoles` — never throws); `resolvesAsStaff` + dev/test-only `STAFF_USER_IDS` override; `tenantContextFromRequest` overlays `isStaff` (prod = realm role only, fail-closed), so `attestReport` (report FINAL in prod) and `moderateDispute` (QA moderation) stop being 409-forever — exit-proven through the real request path with a staff-role claim (route test + `scan/exit.test.ts`) plus unchanged fail-closed 409s for non-staff. Portal full suite: 349 tests (`npx vitest run` in `portal/`).
- **Phase 7 DONE** (pluggable CVE source + Greenbone adapter): `CVESource` seam (protocol + `CVEData`, fake-source checkpoint — engine reads only through `lookup`); `GreenboneSource` cache reader (`$GREENBONE_FEED_PATH`, bare `product:` keys match any version, ranges via `match_ranges`); pure GMP `get_nvts` XML → cache parser (`greenbone_export.py`, fixtures only); `scripts/update_greenbone.py` (live GMP fetch via python-gvm OR `--gmp-xml`, temp+rename atomic writes, empty export never clobbers); engine default = GreenboneSource when the cache exists, else legacy CPEMapper demo fallback (dev/test unchanged). Exit-proofed fixture end-to-end: CLI cache → `CVE-2022-1292` critical for openssl 3.0.1. Live verification against the user's Greenbone install is the pending manual step. Scanner full suite: 70 tests (`make test` in `scanner/`, lint clean). Portal untouched (349/349).
- **Phase 8 DONE (2026-09-05)** — real Keycloak login (8a) + scanner integration gate restored (8b):
  - **8a — Keycloak real-login deployment:** local docker realm on this host (`portal/docker/keycloak/` — `docker-compose.yml` starts Keycloak 26.0.7 on 127.0.0.1:8080, committed realm export `realm.json`: realm `asv-portal`, confidential client `asv-portal` (secret `dev-asv-client-secret-change-me`), realm role **`asv-staff`**, dev users `staff-user`/`staff123` (staff role) + `regular-user`/`user123` (no roles), audience mapper so access tokens carry `aud: asv-portal`); portal `.env` wired (issuer/client/secret/`STAFF_ROLE=asv-staff`); `verify-login.sh` mints + dumps real tokens. **Exit proven LIVE** (`src/lib/scan/exit.live.test.ts`, no mocks — real jose against the running realm's JWKS): real staff login → `verifyToken` OK → `tenantContextFromRequest` isStaff → `/attest` → 200 + report FINAL in prod; real non-staff login → `/attest` → **409**. The live suite skips cleanly when Keycloak is unreachable, so the committed suite stays green without docker. Portal full suite: 352 tests (`npx vitest run` in `portal/`; 349 unit + 3 live).
  - **8b — scanner `make test-integration` restored:** `tests/fixtures/docker-compose.yml` (postgres:17 on 127.0.0.1:54329, db `asv_scanner_it`) + `tests/integration/` (5 real-postgres model round-trip tests: init_db tables, full Customer→Scan→Target→Finding→Evidence→ScopeAudit graph across commit+reload, scope-audit records, DB-enforced ON DELETE CASCADE, session round-trip). `conftest.py` probes the DB and **skips cleanly when docker is absent** (gate exits 0); `make test` ignores the dir so the unit gate stays docker-free. Verified both ways on this host: `make test` = 70 passed; `make test-integration` = 5 passed with docker, 5 skipped (exit 0) without.
- **Runnability status (verified 2026-09-06, fresh checks):**
  - ✅ Portal backend + API (Phases 1–8 + follow-ups) — done, 416 portal tests (413 passed + 3 live-Keycloak skipped without docker) green + tsc clean
  - ✅ Portal UI: assets, scope, scanners, reports (Phase 5) — done, live dashboard pages; **real cookie-session UI login (Keycloak code flow) live-verified** (/team 200 with asv_session cookie, registry row recorded, revoke → 200 → next request fails closed to /sign-in)
  - ✅ Scanner executor + manifest flow (Phase 3b) — done, 86/86 scanner unit tests green + 5 real-postgres integration tests + lint clean; **banner→product alias lookup live vs real cache** (nginx 1.18.0 → 3 CVEs)
  - ✅ Scanner real-scan tooling on this host — nmap 7.98 (system) + python-nmap 0.7.1 (scanner/.venv) + testssl.sh 3.2.4 (scanner/scripts/testssl.sh-3.2.4, on PATH via a `.venv/bin` symlink; dir gitignored); real black-box smoke vs 127.0.0.1:8443 = COMPLETED with nmap banner + testssl.sh TLS grading parsed into tls_version/cipher_strength
  - ✅ Prod staff attestation — `KeycloakUser.roles` from the realm-role claim + `tenantContextFromRequest` `isStaff` overlay (Phase 6); reports can reach FINAL in prod for staff, fail-closed for everyone else
  - ✅ Real CVE data (source seam live) — engine reads through `CVESource` (5b checkpoint); `GreenboneSource` serves the Greenbone-built cache when present (`$GREENBONE_FEED_PATH`), CPEMapper demo fallback only when no cache exists
  - ✅ Keycloak running for real logins — docker realm `asv-portal` on 127.0.0.1:8080 (Phase 8a); real staff/regular password grants mint tokens that verify against the realm JWKS; live attest exit proven (exit.live.test.ts); cookie-session UI login live-verified
  - ✅ Greenbone CVE source — `CVESource` seam + `GreenboneSource` cache reader + GMP export pipeline (Phase 7); scheduled refresh script + make target added (follow-up DONE)
  - ✅ Secret bootstrap guards — portal instrumentation + scanner create_app refuse placeholder/dev creds in prod (follow-up DONE)
- **Scanner Dashboard DONE + MERGED (2026-09-12):** React+Vite dashboard at `scanner/web/` (spec/plan: `docs/superpowers/{specs,plans}/2026-09-06-asv-scanner-dashboard-*`). All 8 plan phases: shell; auth (Zustand + login page + RequireAuth/RequireRole guards, role-aware Rail/TopBar); typed API client (`api/{client,me,customers,scans,findings,sar}.ts`); Watch (2.5s poll, severity histogram, activity feed); Scans list (client filters + sortable table, severity glyphs, PCI bar); scan detail (targets/ports, findings + Inspector w/ QSA suppression, SAR download gated to completed); Customers list/detail (scope CIDRs + audit timeline); polish + **50 vitest tests**. Backend additions: identity `/v1/me`, top-level `/v1/scans`, scope-audit, finding suppress, role enforcement on legacy routes (spec §2), `ScanHistoryItem.customer_id/customer_name/severity_counts`, `FindingResponse.cvss_vector/raw_evidence/suppression_reason`. Gates: web `tsc -b` clean, oxlint 0/0, 50/50 vitest, build OK; scanner `make lint` clean + 86/86 pytest.
  - **Merged into `customer-portal` on 2026-09-12** (came from branch `scanner-dashboard` @ `d05c3cb`, 21 commits). The older "pending merge-to-main review" note is superseded — the working line is `customer-portal`.
  - **Verified LIVE with a genuine scan (2026-09-12, this host):** fixture postgres + real uvicorn (`API_BEARER_TOKEN=dev-token`, real 246 MB `greenbone_cves.json` via `GREENBONE_FEED_PATH`) driven through the dashboard. A real nmap scan of **scanme.nmap.org (45.33.32.156)** completed **FAIL** in ~20s — ports 22 (OpenSSH 6.6.1p1 Ubuntu 2ubuntu2.13), 80 (Apache httpd 2.4.7), 9929, 31337 — with **31 findings, 4 critical** (CVE-2023-38408 9.8, CVE-2016-1908 9.8, CVE-2026-60002 9.4, CVE-2008-3844 9.3) plus apache CVE-2010-1151. The customer scope was seeded to the REAL target (`45.33.32.156/32`), **not** widened to `0.0.0.0/0`, so the production scope gate still applied. This supersedes the earlier "0 findings / PASS" note — that run had no CVE cache loaded, so it could not have found anything.
  - **Local run on this host:** dashboard `http://127.0.0.1:5173` (token `dev-token`), scanner API `http://127.0.0.1:8005`, fixture DB docker `asv-scanner-it` on 54329. Worktree: `.worktrees/scanner-dashboard/`.
  - **Superseded friction record (2026-09-06):** the build was paused because the demo showed fictional/unreachable targets and canned findings — the user wanted one simple working scan ("all i wanted was an ASV scanner"). RESOLVED on 2026-09-12 by running a genuine scan of the real nmap test host and presenting that result. Standing lesson: no fictional targets in a demo, no walls of text, no unrequested actions.
- **CVE source decision (user 2026-09-02):** user installs Greenbone themselves — Greenbone Community Edition ships CERT + SCAP + GVMD_DATA feeds, so it can be the real CVE source via a `GreenboneSource` adapter (CVESource checkpoint). NVD self-hosted mirror spec (`docs/superpowers/specs/2026-09-02-phase5b-nvd-mirror-design.md`, committed `98b2883`, NOT pushed) is the optional offline/backup path — build it only if Greenbone is not the route.

## Environment notes

- **Test DB:** docker postgres `ds-asv-pg` on port 5433, db `asv_portal`, roles `asv` (superuser/admin, via `ADMIN_DATABASE_URL`) and `asv_app` (app, RLS-subject, via `DATABASE_URL`). `.env` in `portal/` is gitignored — copy from `.env.example` and set real passwords for local work.
- **pnpm is broken in this sandbox** (read-only store) — use `npm`/`npx`. Cache: `npm install --cache /home/cchock/projects/.npm-cache`.
- **`MANIFEST_SECRET`** is required in prod (scan-manifest HMAC signing/verification); dev falls back to `"dev-manifest-secret"` (documented in `src/lib/scan/manifest.ts`).
- **Greenbone CVE cache envs (scanner):** all `GREENBONE_*` values can live in **`scanner/greenbone.env`** (gitignored — copy `greenbone.env.example`; one editable file to change the endpoint later; real env vars still win; `$GREENBONE_CONFIG` points at another file). Defaults: `GREENBONE_FEED_PATH` cache file `scanner/data/greenbone_cves.json` (dir gitignored). `GREENBONE_CONNECTION` selects transport: `ssh` (default; python-gvm `SSHConnection`, password auth only — `GREENBONE_SSH_USER/PASSWORD` (+`GREENBONE_SSH_AUTO_ACCEPT=1`); python-gvm defaults the SSH user to `gmp` so the SSH vars are REQUIRED), `tcp` (plain GMP over TCP — pair with `ssh -fN -L 127.0.0.1:9390:/run/gvmd/gvmd.sock user@host`), or `unix` (local `/run/gvmd/gvmd.sock`, `GREENBONE_SOCKET`). GMP credentials always `GREENBONE_USER`/`GREENBONE_PASSWORD`. Live endpoint (2026-09-05): blue = Kali VPS 172.197.208.78 (GVM 25.04, gvmd 26.24, GSA :9392 loopback), reachable via the ssh -L forward above as `cchock` (key `~/.ssh/ASV_key.pem`; also in `_gvm` group, passwordless sudo).
- **Prisma 7 `migrate dev` is non-interactive-unfriendly** — use `npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma` + `migrate deploy` (Prisma 7.10 removed `--from-url`; `--from-config-datasource` reads the admin datasource pinned in `prisma.config.ts`). Generated client is gitignored: run `npx prisma generate` after a clean clone.
- **After any `migrate diff`**, verify the partial unique index `Asset_active_unique` (`organizationId`, `type`, `canonicalIdentifier`) `WHERE "lifecycleState" <> 'retired'` is still present in the generated migration — `migrate diff` may drop it as drift since partial indexes are not expressible in `schema.prisma`. Re-append it if the diff drops it.
- **App connects as `asv_app`** (fail-closed grants, RLS always ON). Admin ops (test seeding/cleanup) go through a second client from `ADMIN_DATABASE_URL`.
- **`set_config('app.tenant_id', ...)` is transaction-scoped** — RLS context must be set inside `prisma.$transaction` with the tx client.
- **`next build` requires `APP_MODE=prod`** (prod-lock guard, spec §6). CI must export it.

## GitHub push gotchas (learned the hard way)

- **Secret scanner blocks pushes.** It flags fake examples: `sk_live_*` placeholders (openapi.yaml), `postgresql://user:pass@` URLs (even localhost), `PASSWORD 'x'` in migrations. Sanitize to `CHANGE_ME` **in history** (`git filter-branch`) — a tip-only fix does NOT clear it.
- **Check the FULL push error first**: `git push > /tmp/push.txt 2>&1; cat /tmp/push.txt` — it names the exact commit/path/secret type. Read that before guessing.
- `gh` token lacks `delete_repo` scope. Credential helper is per-repo: `git config credential.helper "!gh auth git-credential"`.
- Reference dump (sample Qualys PDF, ASV User Flow.drawio, t3 JS/SQL) was removed from git — kept tripping the scanner. Backed up at `/home/cchock/projects/ds-asv-ref-backup/`.

## Working agreements (user explicitly called these out)

1. **NEVER report system state from memory, cache, or earlier in the session.** Git status, test counts, push state, "clean" — always re-run the check at that moment. Cache-recall of status is a bug. **Do not refer to cache at all.**
   - **Exception — key data (read the FILE, never the cache):** durable facts live in AGENTS.md / MEMORY.md (repo URL, DB roles/ports, next-phase, follow-up lists, backup paths). When you need them, **read the file** — do not recall them from memory. If a stored fact looks stale or contradicts a fresh check, the fresh check wins and the file should be updated.
2. **Check the actual error/output before responding** — don't guess, don't offer menus of guesses.
3. **Shorter, plainer answers.** User prefers direct + visual. No walls of text, no status-table spam.
4. **Make calls that are mine to make.** Only ask when only the user can answer.
5. User makes spelling errors — read for intent, never nitpick.

## Known follow-ups (deferred, tracked)

- **DONE — live Greenbone verify (2026-09-05).** Endpoint = **purple** (74.156.0.13): dockerized GVM (**greenbone-community-container** 22.4 stack) with ALL docker+containerd storage on the 1 TB `/home` mount (`~/projects/gvm`; root 30G disk left untouched). GMP `admin` auth verified; 186,323 NVTs + full SCAP/CERT feeds imported. **Live findings:** gvmd 26.24 **ignores `get_nvts` filters and stalls on `details=` responses** (and rejects python-gvm's `filter_string` attribute) — live-verified with both the scanner client and the official `gvm-cli`. Working data path = `update_greenbone.py --cpe-tsv [--ranges-tsv]` from gvmd's own postgres (`scap.cpes`×`scap.cves` + `scap.cpe_match_*` version ranges). **CPE→CVE matching QUALITY FIXED (fef6f68):** TSV path emits product-level (bare) keys only; lookup prefers version-precise **ranges over bare**; excl-start/incl-end rows skipped (never guessed). Cache now = **54,219 product-level + range records** at `scanner/data/greenbone_cves.json`. Verified: `lookup('nginx','1.18.0')` → `CVE-2021-23017` (+CVE-2021-3618, CVE-2025-23419); `nginx 1.27.0` → CVE-2025-23419 only; `openssl 3.0.1` → 55 CVEs; engine finding for nginx 1.18.0 = CVE-2021-23017 (high). **Remaining after follow-ups 2+3 landed:** (1) revisit the NVT-detailed `get_nvts` path when gvmd fixes serving; (4) note: openssl 3.0.1's big range set includes pre-3.0 CVEs — NVD range semantics, not our overlay.
- **DONE — scanner banner→product alias + scheduled refresh (2026-09-06, 9d73886):** follow-up 2 (scheduled refresh) and 3 (service→product alias) from the Greenbone record. `app/scoring/banner_aliases.py` resolves banner → cache product key (explicit nmap `product` field first, product-like service names via `SERVICE_ALIASES`; never invents) and strips the leading product token from display versions (`nginx 1.18.0` → `1.18.0`); engine's unauthenticated path + blackbox connector use it. Pre-fix banner `https`+nginx missed the cache; now `https/nginx 1.18.0` → CVE-2021-23017 etc. via the real cache. `scripts/refresh_greenbone_cache.sh` + `make refresh-greenbone`: pulls the two documented gvmd-postgres dumps (SQL in `greenbone_export` docstrings) and rebuilds atomically; fails loud (exit 1) when `GREENBONE_PG_HOST` unset — never clobbers a good cache. Scanner suite: 86 tests (`make test`), lint clean, 5 integration tests.
- **DONE — cookie sessions bound into the Session registry (2026-09-06, df5d9d0):** real Keycloak authorization-code UI login. `src/lib/auth/session-cookie.ts` (portal `asv_session` httpOnly cookie carrying the Keycloak access token + OAuth state cookie + authorize/exchange helpers); `getKeycloakUser`/`provisionKeycloakUser`/`tenantContextFromRequest`/`sessionMetaFromRequest` fall back to the cookie when no `Authorization` header, so every dashboard page + API honors cookie auth (server `headers()` includes Cookie). New routes `/api/auth/login` → Keycloak, `/api/auth/callback` (exchange + REAL verifyToken + provision + set cookie), `/api/auth/logout` (revokes the registry row + clears cookie). Sign-in page → "Sign in with Keycloak"; sidebar has Sign out; `/access` marks the current session "you". **LIVE exit (running realm):** code flow → cookie → `/team` 200 (previously redirect); request recorded a Session row keyed by the cookie token hash; revoke via `/api/v1/sessions/:id/revoke` → 200; the same cookie then fails closed to `/sign-in`. Portal suite: 373 tests, tsc clean.
- **DONE — env/Vault secret bootstrap (2026-09-06, 40f9e0f):** non-dev runs refuse hardcoded/placeholder credentials at startup. Portal `src/lib/secret-bootstrap.ts` (+ instrumentation register): APP_MODE=prod rejects DB URLs that are unset / carry the local `:asv@` password / are `CHANGE_ME`, and rejects dev `MANIFEST_SECRET` + dev Keycloak client secret. Scanner `app/config_guard.py` (+ `create_app`): APP_MODE=prod rejects placeholder `DATABASE_URL`, `dev-manifest-secret`, `dev-token`. Dev/test untouched. Gates: portal 373 + tsc clean; scanner 86 + lint clean.
- **DONE — pre-existing tsc errors (2026-09-06, 521b13f):** `npx tsc --noEmit` clean. Fixed next.config.ts duplicate `output: standalone`; tsconfig target ES2017 → ES2022 (BigInt literals); `net.isIP` single-arg calls (runtime unchanged); org + team route-test mock typings. Portal suite 352→352.
- Open Greenbone threads: NVT-detailed `get_nvts` path blocked on gvmd serving (revisit when fixed); openssl 3.0.1 range-set semantics note. Plus a parallel uncommitted scanner-dashboard design/plan exists in `docs/superpowers/{specs,plans}/2026-09-06-asv-scanner-dashboard-*.md` (created by another actor, NOT part of these commits).
